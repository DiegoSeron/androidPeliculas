package com.example.peliculas_api;

import androidx.appcompat.app.AppCompatActivity;
import androidx.recyclerview.widget.RecyclerView;

import android.content.Intent;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.EditText;
import android.widget.ImageView;
import android.widget.Spinner;

import com.example.peliculas_api.entities.Index;
import com.example.peliculas_api.entities.Peliculas;
import com.example.peliculas_api.view.LstPeliculasContract;

import java.util.ArrayList;

/*public class Cines extends AppCompatActivity implements LstPeliculasContract.View {

    ImageView flecha;
    String direccion;
    int capacidad;
    int butacasLibres;
    int butacasOcupadas;
    ArrayList<Peliculas> lstCines;
    RecyclerView recyclerCines;
}


    /*@Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_cines);
*/

  /*      initComponentes();
        initPresenter();
        initData();*/
/*
        flecha = (ImageView)findViewById(R.id.idFlechac);
        direccion = (String)findViewById(R.id.);
        capacidad = (int)findViewById(R.id.);
        butacasLibres = (int)findViewById(R.id.);
        butacasOcupadas = (int)findViewById(R.id.);

        flecha.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View view) {
                Intent screenChanger = new Intent(getBaseContext(),
                        MainActivity2.class
                );
                startActivity(screenChanger);
            }
        });

*/
 /*   }

    private void initPresenter() {
    }

    private void initComponentes() {
        flecha = (ImageView)findViewById(R.id.idFlechac);
        lstCines = new ArrayList<>();
    }

    private void initData(){

    }

    @Override
    public void successLstPeliculas(ArrayList<Index> lstIndex) {

    }

    @Override
    public void failureLstPeliculas(String err) {

    }
}*/